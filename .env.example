# OpenClaw Gateway (for local dev, Gateway runs on 63362)
OPENCLAW_GATEWAY_WS_URL=ws://127.0.0.1:63362

# Gateway auth token (if your Gateway requires it)
# OPENCLAW_GATEWAY_TOKEN=your_token_here

# Optional: custom path to OpenClaw session files for cost parsing
# OPENCLAW_SESSIONS_PATH=/path/to/.openclaw

# Server port
PORT=3333
